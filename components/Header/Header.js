import Head from 'next/head'

const Header = () => (
    <Head>
        <title>MSIT</title>
        <meta name="description" content="Microsun IT" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
)

export default Header